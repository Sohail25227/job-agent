# Core
httpx>=0.27
tenacity>=8.2
# Verify TLS against the OS keychain: required on networks that intercept HTTPS
truststore>=0.9
PyYAML>=6.0
pydantic>=2.7
python-dotenv>=1.0
SQLAlchemy>=2.0
rapidfuzz>=3.9
# Postgres driver, used when DATABASE_URL points at one. SQLite needs nothing,
# so local development works without it, but a deploy does not.
psycopg[binary]>=3.1

# CLI + output
typer>=0.12
rich>=13.7

# Web dashboard
fastapi>=0.111
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9
# Signs the session cookie for the dashboard login
itsdangerous>=2.1.2

# Scheduling
APScheduler>=3.10

# Resume: read an uploaded PDF or DOCX so jobs can be scored against it
pypdf>=4.2
python-docx>=1.1

# Optional LLM: structures the uploaded resume, suggests related job titles
openai>=1.30
